[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "algrow"
version = "0.5"
authors = [
  { name="Marcus McHale", email="marcus.mchale@universityofgalway.ie" },
]
description = "Alpha shape defined colourspace for automated growth rate determination in multiplexed images"
readme = "README.md"
license = { file="LICENSE.txt" }
requires-python = ">=3.9"  # tested with 3.9 and 3.10 #todo test with 3.12
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: GNU General Public License v3 (GPLv3)",
    "Operating System :: OS Independent",
]
dependencies = [
    [
        "configargparse",
        "numpy",
        "pandas",
        "scipy",
        "scikit-image",
        "alphashape",
        "matplotlib",
        "open3d"
    ]
]

[project.urls]
"Homepage" = "https://github.com/marcusmchale/algrow"